{
  "name": "pdf-editor",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "sharp": "^0.32.0"
    "pdf-lib": "^1.17.1",
    "serve": "^14.0.1"
  },
  "scripts": {
    "start": "serve dist",
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -R src dist/src"
 "postinstall": "mkdir -p dist"
  }
}
