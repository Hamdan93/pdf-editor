{
  "name": "pdf-editor",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "scripts": {
    "start": "serve -s dist",
    "build": "mkdir dist && cp index.html dist/index.html && cp -R src dist/src"
  }
}
